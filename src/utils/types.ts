export type Category = {
  id: number;
  name: string;
  slug: string;
  color: string;
  image: string;
  description: string;
};

export type District = {
  id: number;
  name: string;
  slug: string;
};
